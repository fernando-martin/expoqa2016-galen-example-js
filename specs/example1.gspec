## Basic specs

# Declaring objects

@objects
    header      id    header
    footer      id    footer
    side-panel  id    side-panel

# Declaring sections

= Header =
    header:
        height 100px

= Content =
    footer:
        "should be grey" css background-color is "rgba(208, 208, 208, 1)"

= Side panel =
    side-panel:
        width 250 to 300 px
                