## Galen-extras
@import galen-extras/galen-extras-rules.gspec

# Declaring objects

@objects
    menu-item-*    css    #menu li a

# Declaring sections

= Menu =
        | amount of any menu-item-* should be 4
        | every menu-item-* has height ~ 50px
        | menu-item-* are aligned horizontally next to each other with 0 to 25px margin


                